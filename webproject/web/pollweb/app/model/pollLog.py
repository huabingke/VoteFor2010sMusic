
class PollLog():
    def __init__(self, user_id, poll_info_id, sex, select_id):
        self.user_id = user_id
        self.poll_info_id = poll_info_id
        self.sex = sex
        self.select_id = select_id